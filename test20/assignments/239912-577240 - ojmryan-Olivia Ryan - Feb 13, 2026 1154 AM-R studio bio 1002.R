#Name: Olivia Ryan
#Biol R-Studio Assignment 2
#Date: February 12, 2026
# question 1: install.packages(dplyr

moosedata <- read_csv("Desktop/Biol1002_Rassignment/MoosePopulation.csv")
View(moosedata)
moosedata <- read.csv("MoosePopulation.csv")
library(dplyr)
na.omit(StraitOfBelleIsleBarrens)
na.omit()
moose_clean <- na.omit(moosedata)
View(moose_clean)
View(moose_clean)
View(moose_clean)
moose_sel <- select(moose_clean, Ecoregion, Year, Area, Estimated_Moose_Pop)
moose_sel <- (moose_clean, Ecoregion, Year, Area, Estimated_Moose_Pop)
select(data, length)
select(year, area)
moose_sel <- select(moose_clean, Ecoregion, Year, Area, Estimated_Moose_Pop)
head(moose_sel)
moose_sel <-(moose_clean, Ecoregion, Year, Area, Estimated_Moose_Pop)
moose_clean <- moose_clean %>%
  +     rename(Estimated_Moose_Pop = `Estimated Moose Pop`)
moose_clean <- moose_clean
     rename(Estimated_Moose_Pop = `Estimated Moose Pop`)
     moose_sel <- select(moose_clean, Ecoregion, Year, Area, Estimated_Moose_Pop)
     colnames(moose_clean)
     moose_sel <- select(
            moose_clean,
            Ecoregion,
            Year,
            Area,
            Estimated_Moose_Pop
        )
     moose_sel <-(
       +     moose_clean,
       moose_sel <-(
         +     moose_clean,
         moose_sel <- dplyr::select(
               moose_clean,
               Ecoregion,
                Year,
                Area,
                Estimated_Moose_Pop
          )
         head(moose_sel)
         moose_sel <- moose_clean[, c(
           +     "Ecoregion",
           +     "Year",
           +     "Area",
           +     "Estimated_Moose_Pop"
           + )]
         
            colnames(moose_sel)
             "Ecoregion" "Year" "Area" "Estimated_Moose_Pop"
             "Ecoregion""Year""Area""Estimated_Moose_Pop"
             "Ecoregion""Year""Area""Estimated_Moose_Pop"
             moose_sel <- moose_clean[, c(
               +     "Ecoregion",
               +     "Year",
               +     "Area",
               +     "Estimated_Moose_Pop"
               + )]
              
                
                colnames(moose_sel)
                moose_sel <- select(moose_clean, Ecoregion, Year, Area, Estimated_Moose_Pop)
                View(moose_sel)
                 View(moose_clean)
                 View(moose_sel)
                 View(moose_clean)
                 moose_sel <- moose_clean[, c(
                  +     "Ecoregion",
                  +     "Year",
                  +     "Area",
                  +     "Estimated_Moose_Pop"
                  + )]
                 
                   colnames(moose_sel)
                   write.csv(moose_sel, "moose_sel.csv", row.names = FALSE)
                    
                      min()
                      min(year)
                      min(1904)
                      year_min <- min(moose_clan$Year)
                      max(41250)
                      moose_max <- )moose_clean$Estimated_Moose_Pop
       mutated(MooseDensity)
       data2 = mitate(data, site_double = site * 2)
      
       moose_clean <- moose_clean %>%
         +     mutate(MooseDensity = Estimated_Moose_Pop / Area)
       moose_clean <- moose_clean %>%
         +     mutate(MooseDensity = Estimated_Moose_Pop / Area)
       head(moose_clean)
       colnames(moose_clean)
       colnames(moose_clean)
       moose_clean <- mutate(moose_clean,
                             +                       MooseDensity = Estimated_Moose_Pop / Area)
       colnames(moose_clean)
       
       "MooseDensity"
       install.packages("dplyr")
       
       moose_clean <- mutate(moose_clean,
                             +                       MooseDensity = Estimated_Moose_Pop / Area)
       colnams(moose_clean)
       mutate(MooseDensity)
       moose_clean <- dplyr::mutate(
         +     moose_clean,
         +     MooseDensity = Estimated_Moose_Pop / Area
         + )
       moose_sel$MooseDensity <- moose_sel$Estimated_Moose_Pop / moose_sel$Area
          colnames(moose_sel)
          moosedata2 <- moosedata
          
             moosedata2 <- mutate(moose_sel, MooseDensity = Estimated_Moose_Pop / Area)
          
             plot(moosedata2$Year, moosedata2$MooseDensity, 
                       xlab = "year", 
                      ylab = "Moose per sq km", 
                      main = "Moose density in Newfoundland ecoregions over time")
             # Assuming your data is saved in a CSV called "moose_data.csv"
              data <- read.csv("moose_data.csv")
             # Create vectors for Year and average MooseDensity (calculated from your table)
              
                Year <- c(1904, 1940, 1960, 1980, 1992, 2020)
             
               MooseDensity <- c(
                 +     0.0005882353,  # 1904
                 +     0.5253352941,  # 1940
                 +     0.8878948159,  # 1960
                 +     0.6150231049,  # 1980
                 +     1.0127260948,  # 1992
                 +     1.5331275843   # 2020
                 + )
              
                moosedata2 <- data.frame(Year, MooseDensity, Ecoregion)
                
              
                plot(moosedata2$Year, moosedata2$MooseDensity,
                      +      type = "o",                         # makes it a line graph with points
                      +      xlab = "Year",
                      +      ylab = "Moose per sq km",
                      +      main = "Moose Density in Newfoundland Ecoregions Over Time")
              
                
                plot(moose_west$Year, moose_west$MooseDensity,
                     +      type = "l",
                     +      xlab = "Year",
                     +      ylab = "Moose per sq km",
                     +      main = "Moose Density Over Time in Western Forests")
               
                filter(Western_Forests)
                filter(Western_Forests)
               
                moose_west <- filter(moosedata2, Ecoregion == "Western_Forests")
                moose_west <- filter(moosedata2, == "Western_Forests")
                moose_west <- filter(moosedata2, "Western_Forests")
                moose_west <- filter(moosedata2, Ecoregion == "Western_Forests")
                names(moosedata2)
                moose_west <- filter(moosedata2, Ecoregion == "Western_Forests")
                moose_west <- filter(moosedata2, Ecoregion == "Western_Forests")
                ls()
                moose_west <- filter(moosedata, Ecoregion == "Western_Forests")
                moose_west <- filter(Western_Forests)
                moose_west <- filter(moose_sel, Ecoregion == "Western_Forests")
                names(moose_sel)
                names(moose_sel)
                moose_west <- filter(moose_sel, `Eco region` == "Western_Forests")
               
                moose_west <- filter(moose_sel, Ecoregion == "Western_Forests")
                moose_west <- filter(moose_sel, Ecoregion == "Western_Forests")
                head(moose_west)
                # Show column names with quotes to see hidden characters
                 dput(names(moose_sel))
            
                head(moose_west)
                names(moose_sel)[grep("Ecoregion", names(moose_sel))]
               
                # Replace the column name below with exactly what grep showed
                 moose_west <- filter(moose_sel, `Ecoregion ` == "Western_Forests")
                # Replace the column name below with exactly what grep showed
                 moose_west <- filter(moose_sel, Ecoregion == "Western_Forests")
                names(moose_sel)[grep("Ecoregion", names(moose_sel))] <- "Ecoregion"
                 
                   moose_west <- filter(moose_sel, Ecoregion == "Western_Forests")
                
                ecolumn <- names(moose_sel)[grep("Ecoregion", names(moose_sel))]
                 names(moose_sel)[names(moose_sel) == ecolumn] <- "Ecoregion"
                 moose_west <- filter(moose_sel, Ecoregion == "Western_Forests")
                plot(moose_west$Year, moose_west$MooseDensity,
                     +      type = "l",                         # line graph
                     +      xlab = "Year",
                     +      ylab = "Moose per sq km",
                     +      main = "Moose Density Over Time in Western Forests Ecoregion")
                moose_west <- filter(moose_sel, Ecoregion == "Western_Forests")
                # Keep only rows where Ecoregion is Western_Forests
                 moose_west <- filter(moose_sel, Ecoregion == "Western_Forests")
                head(moose_west)
                plot(moose_west$Year, moose_west$MooseDensity,
                     +      type = "l",                    # line graph
                     +      xlab = "Year",                 # x-axis label
                     +      ylab = "Moose per sq km",      # y-axis label
                     +      main = "Moose Density Over Time in Western Forests Ecoregion")  # title
                moose_west <- filter(moose_sel, Ecoregion == "Western_Forests")
                filter(Western_Forests)
                filter(5)
                # Load the necessary library
                 
                moose_west <- filter(moosedata2, Ecoregion == "Western_Forests")
                # Example if the column is lowercase
                 moose_west <- filter(moosedata2, ecoregion == "Western_Forests")
                names(moosedata2)
                 View(moosedata2)
                 moose_west <- dplyr::filter(moosedata2, Ecoregion == "Western_Forests")
                View(moose_sel)
                 View(moosedata)
                 names(moosedata2)
        
                 ls() # This lists all objects in your R environment
               
                 # Use the dataset that actually contains the Ecoregion column
                   moose_west <- filter(moosedata, Ecoregion == "Western_Forests")
                # Use the dataset that actually contains the Ecoregion column
                 moose_west <- filter(moose_sel, Ecoregion == "Western_Forests")
                View(moosedata2)
                 # 1. Ensure Ecoregion is included here
                   moose_sel <- select(moosedata, Year, Estimated_Moose_Pop, Area, Ecoregion)
                moose_sel <- select(moosedata, Year, Estimated_Moose_Pop, Area, Ecoregion)
                moose_sel <- select(moosedata, Year, Estimated_Moose_Pop, Area, Ecoregion)
                View(moosedata)
                 View(moosedata2)
                 View(moose_sel)
                 moose_west <- filter(moosedata2, Ecoregion == "Western_Forests")
                 moosedata2 <- data.frame(Year, MooseDensity, Ecoregion)
                 head(moose_west)
                 View(moose_west)
                  plot(moose_west$Year, moose_west$MooseDensity,
                             type = "l",
                             xlab = "Year",
                             ylab = "Moose per sq km",
                              main = "Moose Density Over Time in Western Forests")
                  moose_west
                  moose_west[, c("Year", "MooseDensity")]
                  moose_west <- filter(moosedata2, Ecoregion == "Western_Forests")
                   
                   unique(moosedata2$Ecoregion)
                   moosedata2$Ecoregion <- as.character(moosedata2$Ecoregion)
                   moose_west <- filter(moosedata2, Ecoregion == 5)
                   unique(moosedata2$Ecoregion)
                   "Western_Forests"
                   moose_west <- filter(moosedata2, Ecoregion == "Western Forests")
                    
                      moose_west <- filter(moosedata2, Ecoregion == "Western Forests")
                    moose_west <- filter(moosedata2, Ecoregion == "Western_Forests")
                   
                      moose_west <- filter(moosedata2, grepl("Western", Ecoregion))
                   
                      moose_west
                      moose_west <- filter(moosedata2, Ecoregion == "Western_Forests")
                                      # Use the dataset that actually contains the Ecoregion column
                                         moose_west <- filter(moose_sel, Ecoregion == "Western_Forests")
                                         plot(moose_west$Year, moose_west$MooseDensity,
                                                    type = "l",
                                                    xlab = "Year",
                                                    ylab = "Moose per sq km",
                                                    main = "Moose Density Over Time in Western_Forests Region")
                                         moose_2020 <- filter(moosedata2, Year == 2020)
                                         
                                         moose_2020
                                         moose_2020_high <- filter(moose_2020, MooseDensity > 2.0)
                                         moose_2020_high_byD <- arrange(moose_2020_high, desc(MooseDensity))
                                         
                                         moosefinal <- moosedata2 %>%
                                           filter(Year == 2020) %>%
                                           filter(MooseDensity > 2.0) %>%
                                           arrange(desc(MooseDensity)) %>%
                                           print()
Part 2:                                         
                                         SaplingStudy <- read_csv("Downloads/SaplingStudy.csv")
                                         
                                         View(SaplingStudy)
                                         sap_clean <- na.omit(SaplingStudy)
                                         sap_reg_browse <- sap_clean %>%
                                           group_by(Ecoregion) %>%
                                           summarize(mean_BrowsingScore = mean(BrowsingScore)) %>%
                                           print()
                                         
                                         avg_browse_reg <- sap_reg_browse %>%
                                           arrange(desc(mean_BrowsingScore))
                                         
                                         # The first row has the highest average browsing score
                                         # The last row has the lowest average browsing score
                                         
                                         sap_reg_height <- sap_clean %>%
                                           group_by(Ecoregion) %>%
                                           summarize(mean_Height = mean(Height)) %>%
                                           print()
                                         
                                         sap_reg_height_low <- sap_reg_height %>%
                                           filter(mean_Height < 20) %>%
                                           print()
                                         
                                         # The ecoregions shown above have average tree heights less than 20 cm,
                                         # indicating they are severely browsed by moose.
                                         sap_spe_browse <- sap_clean %>%
                                           group_by(Species) %>%
                                           summarize(mean_BrowsingScore = mean(BrowsingScore)) %>%
                                           print()
                                         avg_browse_spe <- sap_spe_browse %>%
                                           arrange(desc(mean_BrowsingScore))
                                         
                                         # The first species listed has the highest average browsing score
                                         # The last species listed has the lowest average browsing score
                                         
                                         avg_browse_spe
                                         
                                         fir_reg_browse <- sap_clean %>%
                                           filter(Species == "Balsam_Fir") %>%
                                           group_by(Ecoregion) %>%
                                           summarize(mean_BrowsingScore = mean(BrowsingScore))
                                         barplot(fir_reg_browse$mean_BrowsingScore,
                                                 names.arg = fir_reg_browse$Ecoregion,
                                                 xlab = "Ecoregion",
                                                 ylab = "Average Browsing Intensity",
                                                 main = "Average Balsam Fir Browsing by Ecoregion",
                                                 col = "forestgreen",
                                                 cex.names = 0.6)
                                         spruce_reg_browse <- sap_clean %>%
                                           filter(Species == "Black_Spruce") %>%
                                           group_by(Ecoregion) %>%
                                           summarize(mean_BrowsingScore = mean(BrowsingScore))
                                         # Black Spruce shows (lower) browsing intensity compared to Balsam Fir across most ecoregions.
                                         # This suggests moose may have a preference for one species over the other.
                                         
                                         sap_reg_tally <- sap_clean %>%
                                           group_by(Ecoregion) %>%
                                           tally() %>%
                                           print()
                                         sap_spe_tally <- sap_clean %>%
                                           group_by(Species) %>%
                                           tally() %>%
                                           print()
                                         # Black Spruce generally shows slightly lower browsing intensity 
                                         # compared to Balsam Fir across most ecoregions.
                                         # Balsam Fir appears to experience higher overall browsing pressure,
                                         # suggesting moose may prefer Balsam Fir over Black Spruce.
                                         
                                         # The dataset is not evenly distributed.
                                         # Some ecoregions (e.g., North_Shore_Forests) and species (e.g., Balsam_Fir)
                                         # are overrepresented, while others (e.g., StraitOfBelleIsleBarrens and Black_Ash)
                                         # are underrepresented.
                                         # Recognizing bias is important because uneven sampling can distort results
                                         # and lead to inaccurate conclusions about moose browsing patterns.
                                         # Overrepresented groups may appear more important simply due to sample size.
Part 3:                                         
                                         moose_2020b <- moose_clean %>%
                                           filter(Year == 2020) %>%
                                           mutate(MooseDensity = Estimated_Moose_Pop / Area)
                                         moose_sap <- left_join(
                                           moose_2020b,
                                           sap_clean,
                                           by = "Ecoregion",
                                           relationship = "many-to-many"
                                         )
                                         sum_spe_browse <- moose_sap %>%
                                           group_by(Species, Ecoregion) %>%
                                           summarize(
                                             avg_BrowsingScore = mean(BrowsingScore, na.rm = TRUE),
                                             avg_MooseDensity = mean(MooseDensity, na.rm = TRUE)
                                           )
                                         
                                         print(sum_spe_browse)
                                         library(ggplot2)
                                         
                                         ggplot(sum_spe_browse, aes(x = AvgDensity, y = AvgBrowsing, color= Species)) +
                                           geom_point(size = 3) +
                                           
                                           theme_minimal() +
                                           labs(title = "Browsing Intensity Across Moose Density by Species",
                                                x = "Average Moose Density",
                                                y = "Average Browsing Score")
Question 23: a) Yes the figure supposts the hypothosis. At low moose density, borwsing intensity varies strongly among species, which indicates selctive feeding. At higher moose density, borwsing scores converge across species, suggesting more generalist browsing due to an increase in compotition. 
     b) Moose favour Willow the most, as it consistantly shows the highest average browsing scores across densities. Black Spruce is browsed the least, particularly at low moose densities where its browsing scores are lowest or near zero.
     c) Black Ash is not shown because it likely had missing or zero browsing score data after summarizing, resulting in no values to plot in the figure.
collisions2020 <- c(56, 60, 14, 36, 48, 10, 40, 110, 6)

human_pop <- c(18000, 12000, 4000, 75100, 24000, 3500, 32000, 270000, 2300)

study_sites <- c(
  "North_Shore_Forests",
  "Northern_Peninsula_Forests",
  "Long_Range_Barrens",
  "Central_Forests",
  "Western_Forests",
  "EasternHyperOceanicBarrens",
  "Maritime_Barrens",
  "Avalon_Forests",
  "StraitOfBelleIsleBarrens"
)

moose_coll <- data.frame(collisions2020, human_pop, study_sites)
library(dplyr)

moose_coll2 <- moose_coll %>%
  rename(Ecoregion = study_sites)

coll_merge <- left_join(
  moose_2020,
  moose_coll2,
  by = "Ecoregion"
)
plot(
  coll_merge$MooseDensity,
  coll_merge$collisions2020,
  xlab = "Moose Density",
  ylab = "Number of Moose-Vehicle Collisions (2020)",
  main = "Moose Density vs Moose-Vehicle Collisions"
)
plot(
  c(2.5880952, 2.6666667, 0.6022727, 2.0121951, 2.5, 0.5, 0.9600939, 0.9619048, 0.01),
  c(56, 60, 14, 36, 48, 10, 40, 110, 6),
  xlab = "Moose Density",
  ylab = "Number of Moose-Vehicle Collisions (2020)",
  main = "Moose Density vs Moose-Vehicle Collisions"
)
Question #26: There is a generaly positive relationship between moose density and the number of moose-vehicle collisions, with higher densities tending to correspond to more collisions. However, some ecoregions appear as outliers, likely reflecting differences in road density and human traffic rather than moose density alone.
coll_merge_per_capita <- coll_merge %>%
  mutate(coll_per_capita = collisions2020 / human_pop)
plot(
  c(18000, 12000, 4000, 75100, 24000, 3500, 32000, 270000, 2300),
  c(0.00311111, 0.005, 0.0035, 0.00047936, 0.002, 0.00285714, 0.00125, 0.00040741, 0.00260870),
  xlab = "Human Population",
  ylab = "Moose Collisions per Capita",
  main = "Moose Collisions per Capita vs Human Population"
)
Question #29 The plot shows that collisions per capita tend to be higher in ecoregions with smaller human populations, while densely populated regions have lower per-capita collision rates. This trend makes sense, as rural areas often have more moose habitat intersecting roads and fewer people sharing the collision risk.
